% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{fit_af_glm}
\alias{fit_af_glm}
\title{Quasibinomial GLM of allele frequency over time}
\usage{
fit_af_glm(
  freq_mat,
  sample_meta,
  snp_coords = NULL,
  p0 = NULL,
  p0_weight = 1000,
  fixed_intercept = FALSE
)
}
\arguments{
\item{freq_mat}{A \code{freq_matrix} object: L variants (rows) x S samples
(columns), with column names \code{pop_time_rep} matching \code{sample_meta} (as
built by \code{\link[=sample_info]{sample_info()}}).}

\item{sample_meta}{A \code{sample_info} object with S rows, matching the
columns of \code{freq_mat} in order.}

\item{snp_coords}{Optional \code{snp_coords} object with L rows, providing the
\code{CHROM} and \code{POS} output columns.}

\item{p0}{Optional \code{p0_vec} object of length L: shared allele frequencies
at generation 0. When supplied, each population's fit data gains one
extra observation at \code{t = 0} with \code{value = p0} and weight \code{p0_weight},
anchoring the trajectory at the shared baseline (observed time-0
columns of \code{freq_mat} are never used as model input).}

\item{p0_weight}{Numeric weight given to the \code{p0} observation, default
\code{1000}. Ignored when \code{p0 = NULL}.}

\item{fixed_intercept}{Logical, default \code{FALSE}. When \code{TRUE} (and \code{p0}
is supplied), fits \code{value ~ t - 1 + offset(qlogis(p0))} instead,
pinning the intercept at the shared baseline on the logit scale.}
}
\value{
A data.frame with one row per variant and population, with columns
\code{population}, \code{CHROM}, \code{POS} (the latter two only if \code{snp_coords} is
supplied), \code{estimate} (logistic slope of \code{t}), \code{std.error}, \code{statistic},
and \code{p.value} (the \code{t} term only; no intercept row). Variants whose fit
fails (e.g. all frequencies NA in the population) get \code{NA} statistics.
}
\description{
For each variant and population, fits \code{glm(value ~ t, weights = n, family = quasibinomial(link = "logit"))} to the allele-frequency trajectory over
time points, where \code{value} is the allele frequency, \code{t} the time point in
generations, and \code{n} the per-replicate sample size used as observation
weights. Returns the slope of the logistic trend (\code{t} term) per variant
and population. Adapted from a chunked allele frequency processing script
of the grenenet-phase2 project.
}
\details{
Only time points greater than zero enter the model, with the per-sample
\code{sample_size} as GLM weights; if \code{p0} is supplied, a single extra
observation per population at \code{t = 0} with weight \code{p0_weight} is added
first, so the shared baseline participates in every population's fit.
Frequencies of exactly 0 or 1 are kept (the quasibinomial model handles
boundary values); variants with many fixed observations may still fail
to converge and then yield \code{NA} statistics. The function is pure: to
process a large CSV in batches, first convert it to a parquet dataset
with \code{\link[=csv_to_parquet]{csv_to_parquet()}}, then apply this function per batch and append
the results.
}
\examples{
set.seed(1)
L <- 5
meta <- rbind(expand.grid(population = "AA", time_point = c(0, 1, 2),
                          replicate = c("R1", "R2")),
              expand.grid(population = "BB", time_point = c(0, 1, 2),
                          replicate = "R1"))
meta$sample_size <- 30
meta <- meta[order(meta$population, meta$time_point, meta$replicate), ]
fm <- freq_matrix(matrix(runif(L * nrow(meta)), nrow = L,
                         dimnames = list(NULL, paste(meta$population,
                             meta$time_point, meta$replicate, sep = "_"))))
fit_af_glm(fm, sample_info(meta))
}
