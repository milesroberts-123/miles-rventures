% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{validate_af_dataset}
\alias{validate_af_dataset}
\title{Validate a set of temporal allele frequency dataset objects}
\usage{
validate_af_dataset(
  freq_mat,
  snp_coords,
  p0,
  sample_meta,
  check_colnames = TRUE
)
}
\arguments{
\item{freq_mat}{A \code{freq_matrix} object.}

\item{snp_coords}{A \code{snp_coords} object.}

\item{p0}{A \code{p0_vec} object.}

\item{sample_meta}{A \code{sample_info} object.}

\item{check_colnames}{If \code{TRUE} and \code{freq_mat} has column names, check
they match the \code{pop_time_rep} scheme derived from \code{sample_meta}.}
}
\value{
\code{TRUE}, invisibly, if all checks pass; an error otherwise.
}
\description{
Cross-checks the four dataset objects for compatible dimensions: the same
number of variants (rows of the frequency matrix and coordinates, length
of the initial frequencies) and the same number of samples (columns of the
frequency matrix, rows of the metadata). Optionally checks that frequency
matrix column names follow the \code{pop_time_rep} naming scheme.
}
\examples{
m <- matrix(runif(10), nrow = 5)
fm <- freq_matrix(m)
coords <- snp_coords(data.frame(chrom = rep("1", 5), pos = 1:5 * 100))
p0 <- p0_vec(m[, 1])
meta <- sample_info(data.frame(
  population = c("AA", "BB"), time_point = c(0, 10), replicate = c("R1", "R1")
))
validate_af_dataset(fm, coords, p0, meta)
}
