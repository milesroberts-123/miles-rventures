% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{sign_permute_increments}
\alias{sign_permute_increments}
\title{Sign permute a block of allele frequency changes}
\usage{
sign_permute_increments(pdiff, procedure = "none", windows = NULL)
}
\arguments{
\item{pdiff}{Data.frame or matrix of allele frequency changes (rows =
variants, columns = time intervals).}

\item{procedure}{One of \code{"none"} (return input unchanged), \code{"window"}
(one random sign per genomic window, applied to all variants in the
window), \code{"cell"} (one random sign per matrix entry), or \code{"genome"}
(one random sign per column). Invalid values raise an error.}

\item{windows}{Numeric or character vector of window IDs, one per row of
\code{pdiff}; required when \code{procedure = "window"}.}
}
\value{
The sign-permuted allele frequency changes: same type as the
input for \code{"none"}, a data.frame for \code{"window"} and \code{"cell"}, and a
matrix for \code{"genome"}.
}
\description{
Randomly multiplies allele frequency changes by +1 or -1 at one of several
granularities, for use in randomization tests of linked selection.
Because covariance is signed, flipping signs breaks real shared-direction
structure while preserving per-element magnitudes.
}
\examples{
set.seed(1)
pdiff <- data.frame(d1 = c(-0.1, 0.2, -0.3, 0.4), d2 = c(0.1, -0.2, 0.3, -0.4))
sign_permute_increments(pdiff, procedure = "genome")
}
