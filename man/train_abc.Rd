% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{train_abc}
\alias{train_abc}
\title{Approximate Bayesian computation cross-validation}
\usage{
train_abc(
  train_sample,
  val_sample,
  outcomes,
  summary_stats,
  tol,
  method,
  verbose = FALSE
)
}
\arguments{
\item{train_sample}{Data frame of training simulations; columns selected by
\code{outcomes} and \code{summary_stats}.}

\item{val_sample}{Data frame of validation simulations with the same
columns.}

\item{outcomes}{Character vector naming the outcome (parameter) column(s).}

\item{summary_stats}{Character vector naming the summary statistic
column(s).}

\item{tol}{ABC acceptance tolerance, passed to \code{abc::abc()}.}

\item{method}{ABC method, passed to \code{abc::abc()} (\code{"rejection"},
\code{"loclinear"}, \code{"neuralnet"}, or \code{"ridge"}).}

\item{verbose}{If \code{TRUE}, emit a message for each validation example.}
}
\value{
A data frame with one row per validation simulation and columns
\code{truth}, \code{lwr_cred_int}, \code{pred_mean}, \code{pred_median}, \code{pred_mode}, and
\code{upr_cred_int}.
}
\description{
Evaluates an ABC model the way a neural network is validated: after
"training" on a set of simulated parameter/summary-statistic pairs, each
validation data point is predicted using the training data alone. Each
validation simulation is used as the ABC target in turn, and the posterior
is summarized by its credible interval, mean, median, and mode.
}
\details{
Requires the \code{abc} package (installed separately; it is only suggested by
this package).
}
\examples{
\donttest{
sim_stats <- data.frame(
  Ne = c(100, 200, 500, 1000),
  S1 = c(1, 2, 3, 4),
  S2 = c(5, 6, 7, 8)
)
train_abc(
  train_sample = sim_stats, val_sample = sim_stats,
  outcomes = "Ne", summary_stats = c("S1", "S2"),
  tol = 0.5, method = "rejection"
)
}
}
