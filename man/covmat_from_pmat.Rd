% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{covmat_from_pmat}
\alias{covmat_from_pmat}
\title{Calculate covariances from allele frequency matrix}
\usage{
covmat_from_pmat(
  pmat,
  n = NULL,
  correct_for_n = TRUE,
  standard_by_het = FALSE,
  input_asin_trans = FALSE,
  procedure = "none",
  windows = NULL
)
}
\arguments{
\item{pmat}{Numeric matrix of allele frequencies; the first column is the
initial generation and time points ascend left to right.}

\item{n}{Optional numeric vector of sample sizes (number of chromosomes
sampled per time point); required if \code{correct_for_n = TRUE}. Must have
one entry per time point (i.e., \code{ncol(pmat)}).}

\item{correct_for_n}{Boolean; whether to correct covariances and variances
for finite sample size.}

\item{standard_by_het}{Boolean; whether to standardize the covariance
matrix by heterozygosity via \code{\link[=standard_cov_by_het]{standard_cov_by_het()}}.}

\item{input_asin_trans}{Boolean; whether \code{pmat} holds arcsine-square-root
transformed frequencies (changes the sample size correction formulas).}

\item{procedure}{Sign randomization procedure passed to
\code{\link[=sign_permute_increments]{sign_permute_increments()}}; use \code{"none"} (default) for no
randomization.}

\item{windows}{Optional vector of window IDs, one per row of \code{pmat},
passed to \code{\link[=sign_permute_increments]{sign_permute_increments()}} when \code{procedure = "window"}.}
}
\value{
The (optionally corrected/standardized) covariance matrix of
allele frequency changes.
}
\description{
Computes the covariance matrix of allele frequency changes between
adjacent generations (columns = time intervals), optionally with a sample
size correction, heterozygosity standardization, arcsine-transformed
input, and sign randomization for null comparisons.
}
\examples{
set.seed(1)
pmat <- matrix(runif(15), nrow = 5, ncol = 3)
covmat_from_pmat(pmat, n = c(50, 50, 50))
}
