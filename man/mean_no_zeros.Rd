% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression.R
\name{mean_no_zeros}
\alias{mean_no_zeros}
\title{Mean expression of expressed samples only}
\usage{
mean_no_zeros(x, thresh)
}
\arguments{
\item{x}{A numeric vector of expression values for one gene.}

\item{thresh}{Numeric expression threshold; only values strictly greater
than \code{thresh} contribute to the mean.}
}
\value{
A single numeric value: the mean of the values in \code{x} that are
strictly greater than \code{thresh} and not \code{NA}, or \code{NA_real_} if no value
qualifies.
}
\description{
Computes the mean of an expression vector conditioned on the gene being
expressed at all: values at or below \code{thresh} are dropped before
averaging. Returns \code{NA_real_} when no value exceeds \code{thresh}.
}
\examples{
mean_no_zeros(c(0, 2, 4, 8), thresh = 0)
mean_no_zeros(c(0, 0, 0), thresh = 0)
}
