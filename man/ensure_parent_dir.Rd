% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{ensure_parent_dir}
\alias{ensure_parent_dir}
\title{Create the parent directories of a target file path, if missing}
\usage{
ensure_parent_dir(file_path)
}
\arguments{
\item{file_path}{Path to the target file. Only the folder portion is
created; the file itself is left untouched.}
}
\value{
Invisibly, \code{TRUE} if the folder structure was created, \code{FALSE} if
it already existed.
}
\description{
Takes the path to a target file, extracts its folder portion with
\code{\link[base:dirname]{base::dirname()}}, and creates the full folder structure with
\code{\link[base:dir.create]{base::dir.create()}} if it does not yet exist. Useful before writing
outputs into a not-yet-existing nested directory.
}
\examples{
target <- file.path(tempdir(), "nested", "data_results", "output.csv")
ensure_parent_dir(target)   # TRUE: folders created
ensure_parent_dir(target)   # FALSE: folders already exist
}
