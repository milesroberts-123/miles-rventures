% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{feder_t_test}
\alias{feder_t_test}
\title{Feder-style t-test of allele-frequency changes, pooled across replicates}
\usage{
feder_t_test(
  freq_mat,
  p0,
  sample_meta,
  snp_coords = NULL,
  alternative = c("two.sided", "less", "greater")
)
}
\arguments{
\item{freq_mat}{A \code{freq_matrix} object: L variants (rows) x S samples
(columns), with column names \code{pop_time_rep} matching \code{sample_meta} (as
built by \code{\link[=sample_info]{sample_info()}}).}

\item{p0}{A \code{p0_vec} object of length L: shared allele frequencies at
generation 0, used as the baseline frequency of the first interval.}

\item{sample_meta}{A \code{sample_info} object with S rows, matching the
columns of \code{freq_mat} in order.}

\item{snp_coords}{Optional \code{snp_coords} object with L rows, providing the
\code{CHROM} and \code{POS} output columns.}

\item{alternative}{Character string, one of \code{"two.sided"} (default),
\code{"less"}, or \code{"greater"}: the alternative hypothesis of the t-test.}
}
\value{
A data.frame with one row per variant and population, with columns
\code{population}, \code{CHROM}, \code{POS} (the latter two only if \code{snp_coords} is
supplied), \code{ybar} (mean standardized allele-frequency change),
\code{s2} (sample variance of the pooled changes), \code{n} (number of non-NA
changes pooled across time points and replicates), \code{df} (\code{n - 1}),
\code{tvalue}, and \code{pvalue}. Variants without usable changes in a population
get \code{NA} for the statistic columns.
}
\description{
For each population, computes standardized allele-frequency changes
between adjacent time points, `y = (p_t' - p_t) / sqrt(2 * p_t * (1 - p_t)
\itemize{
\item dt)\code{where}dt\verb{is the generation gap and}p_t\verb{is the frequency at the previous time point (the shared initial frequencies}p0\verb{serve as the baseline for the first interval), pools the}y\verb{values of a variant across all time points and replicates of the population, and runs a one-sample t-test of}H0: ybar = 0` per variant. Adapted from a chunked allele
frequency processing script of the grenenet-phase2 project (Feder et al.
style t-test of temporal allele-frequency change).
}
}
\details{
The test pools across adjacent intervals and replicates, so \code{df} equals
the total number of non-NA allele-frequency increments of the population
(across all time points and replicates) minus 1. Groups with fewer than
two usable changes have an undefined variance and yield \code{NA} statistics.
Frequencies of exactly 0 or 1 make the standardization denominator zero;
such boundary variants should be removed upstream with
\code{\link[=filter_fixations]{filter_fixations()}}. The function is pure: to process a large CSV in
batches, first convert it to a parquet dataset with \code{\link[=csv_to_parquet]{csv_to_parquet()}},
then apply this function per batch and append the results.
}
\examples{
set.seed(1)
L <- 5
meta <- rbind(expand.grid(population = "AA", time_point = c(0, 1, 2),
                          replicate = c("R1", "R2")),
              expand.grid(population = "BB", time_point = c(0, 1, 2),
                          replicate = "R1"))
meta$sample_size <- 30
meta <- meta[order(meta$population, meta$time_point, meta$replicate), ]
fm <- freq_matrix(matrix(runif(L * nrow(meta)), nrow = L,
                         dimnames = list(NULL, paste(meta$population,
                             meta$time_point, meta$replicate, sep = "_"))))
p0 <- p0_vec(runif(L, 0.2, 0.8))
feder_t_test(fm, p0, sample_info(meta))
}
