% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_var_cov_matrix}
\alias{plot_var_cov_matrix}
\title{Heatmap of a variance-covariance matrix}
\usage{
plot_var_cov_matrix(
  covmat,
  include_values = TRUE,
  include_tick_labels = TRUE,
  low_col = "#65014B",
  mid_col = "#F5F0F0",
  high_col = "#0C4C00",
  legend_name = "Cov(Δpi, Δpj)",
  tile_or_raster = c("tile", "raster"),
  discrete_or_continuous = c("continuous", "discrete")
)
}
\arguments{
\item{covmat}{A square matrix with symmetric row and column names.}

\item{include_values}{Print each cell's value (rounded to 4 decimals) on
its tile.}

\item{include_tick_labels}{Show axis tick labels; if \code{FALSE}, all axis
text and panel decoration is removed.}

\item{low_col}{Fill color for the most negative values.}

\item{mid_col}{Fill color at the midpoint (0).}

\item{high_col}{Fill color for the most positive values.}

\item{legend_name}{Legend title.}

\item{tile_or_raster}{\code{"tile"} (white cell borders) or \code{"raster"} (no
borders) geometry.}

\item{discrete_or_continuous}{\code{"continuous"} uses a diverging color
gradient (\code{low_col}/\code{mid_col}/\code{high_col}); \code{"discrete"} uses the scico
\code{"bam"} palette (requires the scico package, a suggested dependency).}
}
\value{
A ggplot object. Save it with \code{\link[=save_plot]{save_plot()}}.
}
\description{
Plots the upper triangle of a square variance-covariance (or correlation)
matrix as a heatmap: one tile per cell pair, filled by the cell value
(continuous green-magenta gradient centered at 0, or a discrete scico
palette). Cell values and/or tick labels can be suppressed for a
publication-style matrix figure.
}
\note{
The default legend title contains a Unicode character (Δ). Saving
with the default \code{pdf()} device cannot render it; use a Unicode-capable
device such as \code{cairo_pdf} (e.g. \code{ggplot2::ggsave(..., device = grDevices::cairo_pdf)}).
}
\examples{
covmat <- matrix(
  c(1.0, 0.3, 0.1,
    0.3, 1.0, 0.5,
    0.1, 0.5, 1.0),
  nrow = 3, byrow = TRUE,
  dimnames = list(c("s1", "s2", "s3"), c("s1", "s2", "s3"))
)
# ASCII legend title keeps the example compatible with the default pdf()
# device; the default "Cov(\u0394pi, \u0394pj)" needs a Unicode-capable device
plot_var_cov_matrix(covmat, include_values = TRUE, legend_name = "Cov")
}
