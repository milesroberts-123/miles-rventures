% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{compare_predictions_truth_scatter}
\alias{compare_predictions_truth_scatter}
\title{Scatterplot comparing predictions to true values}
\usage{
compare_predictions_truth_scatter(
  predictions,
  truth,
  model_label,
  truth_label,
  pred_label,
  color_var,
  color_label
)
}
\arguments{
\item{predictions}{Numeric vector of predicted values.}

\item{truth}{Numeric vector of true values, same length as \code{predictions}.}

\item{model_label}{Model name shown in the plot title.}

\item{truth_label}{Axis label for the true values.}

\item{pred_label}{Axis label for the predicted values.}

\item{color_var}{Numeric vector mapped to point color, same length as
\code{predictions}.}

\item{color_label}{Legend title for the color scale.}
}
\value{
A ggplot object.
}
\description{
Plots predicted values against true values for a continuous variable, with
the identity line (predictions = truth), a linear fit, and the Pearson
correlation with 95\% CI, sample size, and RMSE in the plot title. Pairs
where either variable is NA, NaN, or infinite are dropped before analysis.
}
\examples{
set.seed(1)
truth <- rnorm(100)
compare_predictions_truth_scatter(
  predictions = truth + rnorm(100, sd = 0.5),
  truth = truth,
  model_label = "Example model",
  truth_label = "Truth",
  pred_label = "Prediction",
  color_var = truth,
  color_label = "True value"
)
}
