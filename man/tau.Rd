% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression.R
\name{tau}
\alias{tau}
\title{Cell-type specificity (tau)}
\usage{
tau(x, na_action = c("propagate", "remove", "error"))
}
\arguments{
\item{x}{A numeric vector of expression values for one gene.}

\item{na_action}{How to handle \code{NA} values: \code{"propagate"} returns
\code{NA_real_} if any value is missing; \code{"remove"} computes tau on the
observed values only (returning \code{NA_real_} if fewer than two remain);
\code{"error"} stops on missing values.}
}
\value{
A single numeric value in \code{[0, 1]}, or \code{NA_real_} for
degenerate input (fewer than 2 values, all zeros, or propagated
missingness).
}
\description{
Computes the tau specificity index of an expression vector (Kryuchkov
et al. 2016): 0 means ubiquitous expression across samples, 1 means
expression concentrated in a single sample. The vector is scaled by its
maximum before averaging, so tau is scale-invariant.
}
\examples{
tau(c(1, 1, 1, 1))   # ubiquitous: 0
tau(c(10, 0, 0, 0))  # single sample: 1
tau(c(5, 2, 8, 1))
}
