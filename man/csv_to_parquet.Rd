% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{csv_to_parquet}
\alias{csv_to_parquet}
\title{Convert a large CSV file to a parquet dataset for lazy loading}
\usage{
csv_to_parquet(
  csv_path,
  parquet_dir,
  block_size = 16L * 1024L * 1024L,
  max_rows_per_file = 50000L
)
}
\arguments{
\item{csv_path}{Path to the CSV file to convert.}

\item{parquet_dir}{Path to the output directory of parquet files.}

\item{block_size}{Block size in bytes for reading the CSV. Larger blocks
speed up parsing of wide files. Default is 16 MiB.}

\item{max_rows_per_file}{Maximum number of rows per parquet file in the
output dataset. Default is 50000.}
}
\value{
A lazy \link[arrow:Dataset]{arrow::Dataset} object opened from \code{parquet_dir}.
}
\description{
One-time conversion of a large CSV file into a directory of parquet files
that can be opened with \code{\link[arrow:open_dataset]{arrow::open_dataset()}} and queried lazily (only
the needed chunks are read into memory). If \code{parquet_dir} does not exist
yet, the CSV is read with a large block size and written as parquet in
chunks of \code{max_rows_per_file} rows; otherwise the conversion step is
skipped. In both cases the lazy dataset is returned, so the function is
safe to place at the top of a script that is re-run repeatedly.
}
\details{
Note that an existing but empty or incomplete \code{parquet_dir} is trusted as
a completed conversion, mirroring the \code{dir.exists()} guard this function
wraps.
}
\examples{
\dontrun{
# One-time conversion, then lazy reads on every re-run:
hapfire <- csv_to_parquet(
  "/global/scratch/users/milesroberts/moi_lab_projects/grenenet-phase2/data/SV_SNP_INDEL_allele_frequency_trays.csv",
  "/global/scratch/users/milesroberts/moi_lab_projects/grenenet-phase2/data/kmate_parquet"
)
}
}
