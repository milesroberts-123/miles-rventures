% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{estim_linked_selection_params_new}
\alias{estim_linked_selection_params_new}
\title{Compile table of values in windows for Buffalo and Coop 2019 Ne and VA(1)
estimation, with heterozygosity standardization}
\usage{
estim_linked_selection_params_new(
  pmat,
  n,
  sum_of_het_vec,
  sum_of_het_1,
  mean_ld,
  weight,
  correct_for_n = TRUE
)
}
\arguments{
\item{pmat}{Numeric matrix of allele frequencies excluding the last time
point: one column per interval, holding the frequency at the start of
that interval.}

\item{n}{Numeric vector of sample sizes, one per time point.}

\item{sum_of_het_vec}{Data frame from \code{\link[=sum_of_het_by_t]{sum_of_het_by_t()}}.}

\item{sum_of_het_1}{Scalar: summed heterozygosity of the first time point.}

\item{mean_ld}{Scalar: mean LD (r^2) within the window.}

\item{weight}{Scalar: weight of the window in the pooled analysis.}

\item{correct_for_n}{Logical; apply the sample-size correction
(default TRUE).}
}
\value{
A data frame, one row per unique covariance-matrix value, with the
melted Var1/Var2/value columns, interval labels \code{s} and \code{t}, the
variance indicator \code{b}, the heterozygosity ratio \code{sum_of_het_ratio},
the per-window coefficient \code{a}, and the window-level constants.
}
\description{
As \code{\link[=estim_linked_selection_params]{estim_linked_selection_params()}}, but the covariance matrix is
standardized by heterozygosity before melting.
}
