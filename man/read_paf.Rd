% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{read_paf}
\alias{read_paf}
\title{Read a PAF alignment file}
\usage{
read_paf(file)
}
\arguments{
\item{file}{Path to a PAF file. PAF files written by \code{minimap2 --paf} have
no header row; extra columns beyond the 12 mandatory ones are ignored.}
}
\value{
A tibble with 13 columns: the 12 mandatory PAF fields
(\code{qname}, \code{qlen}, \code{qstart}, \code{qend}, \code{strand}, \code{tname}, \code{tlen},
\code{tstart}, \code{tend}, \code{nmatch}, \code{alen}, \code{mapq} — all but \code{qname}, \code{strand},
and \code{tname} numeric) plus \code{pident} (\code{nmatch / alen}).
}
\description{
Reads a PAF (Pairwise mApping Format) file produced by minimap2, keeps the
12 mandatory columns, coerces the numeric fields, and adds a per-alignment
percent-identity column.
}
\examples{
\dontrun{
paf <- read_paf("hap2_vs_hap1.paf")
}
}
