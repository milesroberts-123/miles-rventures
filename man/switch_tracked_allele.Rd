% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{switch_tracked_allele}
\alias{switch_tracked_allele}
\title{Randomly switch the tracked allele}
\usage{
switch_tracked_allele(freq_mat, p0, idx = NULL)
}
\arguments{
\item{freq_mat}{A \code{freq_matrix} object with L rows (variants).}

\item{p0}{A \code{p0_vec} object of length L.}

\item{idx}{Optional logical vector of length L selecting which variants
to switch (no NAs). Default NULL samples a fresh 50/50 index; pass an
index to reproduce a switch or coordinate it across datasets.}
}
\value{
A list with \code{freq_mat} and \code{p0}, the tracked allele switched on
the shared subset of variants.
}
\description{
Flips the tracked allele (\code{1 - x}) for a random subset of variants,
applying the same subset to both a \code{freq_matrix} and a matched \code{p0_vec}.
NA entries pass through unchanged (1 - NA = NA).
}
\examples{
set.seed(1)
fm <- freq_matrix(matrix(c(0.1, 0.9, 0.3, 0.7), nrow = 2))
p0 <- p0_vec(c(0.4, 0.6))
out <- switch_tracked_allele(fm, p0)
out$p0
}
