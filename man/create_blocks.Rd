% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{create_blocks}
\alias{create_blocks}
\title{Block data by chromosome and position}
\usage{
create_blocks(
  data,
  chrom_col_name,
  pos_col_name,
  window_size,
  sep,
  block_by = "snp"
)
}
\arguments{
\item{data}{Data frame with chromosome and position columns.}

\item{chrom_col_name}{Chromosome column (tidy-evaluated).}

\item{pos_col_name}{Position column (tidy-evaluated).}

\item{window_size}{Block size: SNPs per block if \code{block_by = "snp"}, base
pairs per block if \code{block_by = "base"}.}

\item{sep}{Separator pasted between chromosome and block index to form
the \code{window} label.}

\item{block_by}{Either \code{"snp"} (fixed number of SNPs per block) or
\code{"base"} (fixed physical span per block).}
}
\value{
\code{data} with columns \code{block} (block index within chromosome) and
\code{window} (chromosome-sep-block label).
}
\description{
Assigns each row of a sorted variant table to a block, computed either by
SNP count or by physical distance, and never across chromosome boundaries.
}
\examples{
\dontrun{
blocks <- create_blocks(snp_table, chrom, pos, 15000,
                        block_by = "snp", sep = "_")
}
}
