% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{bind_files}
\alias{bind_files}
\title{Read a list of files and bind their tables together}
\usage{
bind_files(file_list, col_names = NULL, id = "source_file", ...)
}
\arguments{
\item{file_list}{Character vector of file paths, e.g. the output of
\code{\link[=list.files]{list.files()}} with \code{full.names = TRUE}.}

\item{col_names}{Optional character vector of column names passed to
\code{fread}'s \code{col.names}, overriding the headers in the files. \code{NULL}
keeps the file headers.}

\item{id}{Name of the source-file column added to the result, holding
the full path of the file each row came from. \code{NULL} omits the column.}

\item{...}{Additional arguments passed to \code{\link[data.table:fread]{data.table::fread()}}, e.g.
\code{sep}, \code{select}, or \code{header}.}
}
\value{
A data frame with one row per row of every input file, plus the
id column if \code{id} is not \code{NULL}.
}
\description{
Reads each file in \code{file_list} with \code{\link[data.table:fread]{data.table::fread()}} and binds the
resulting tables row-wise into a single data frame. A source-file column
(full paths, in the order given) is prepended unless \code{id = NULL}.
}
\examples{
f1 <- tempfile(fileext = ".txt")
f2 <- tempfile(fileext = ".txt")
writeLines("0.1\t0.9", f1)
writeLines("0.2\t0.8", f2)
bind_files(c(f1, f2), col_names = c("bray_curtis", "cosine"))
}
