% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{extract_samples}
\alias{extract_samples}
\title{Extract samples from an allele frequency matrix}
\usage{
extract_samples(
  freq_mat,
  sample_meta,
  population = NULL,
  time = NULL,
  replicate = NULL
)
}
\arguments{
\item{freq_mat}{A \code{freq_matrix} object.}

\item{sample_meta}{The \code{sample_info} object for \code{freq_mat}.}

\item{population}{Optional character vector of populations to keep.}

\item{time}{Optional integer vector of time points to keep.}

\item{replicate}{Optional character vector of replicates to keep.}
}
\value{
A classed \code{freq_matrix} with columns in metadata order, column
names \code{pop_time_rep}, and attribute \code{"sample_info"} holding the subset
metadata. Errors if no sample matches.
}
\description{
Subsets the columns of a \code{freq_matrix} by population, time point, and/or
replicate (using \code{\%in\%} matching against the sample metadata), rebuilds
column names as \code{pop_time_rep}, and attaches the subset metadata as the
attribute \code{"sample_info"}. The rebuilt names let the result feed directly
into \code{\link[=covmat_pop_pair]{covmat_pop_pair()}} and provide replicate/time labels for
\code{\link[=replicate_gt]{replicate_gt()}}.
}
\examples{
m <- matrix(runif(8), nrow = 4, ncol = 4)
fm <- freq_matrix(m)
meta <- sample_info(data.frame(
  population = c("AA", "AA", "BB", "BB"),
  time_point = c(0, 10, 0, 10),
  replicate = c("R1", "R1", "R1", "R1")
))
extract_samples(fm, meta, population = "AA")
}
