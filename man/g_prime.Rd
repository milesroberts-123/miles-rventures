% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{g_prime}
\alias{g_prime}
\title{G statistic, corrected for selection increasing variance in allele
frequency change}
\usage{
g_prime(times, pmat, N, n, take_abs = FALSE)
}
\arguments{
\item{times}{Numeric vector of time points, one per interval of \code{pmat}
(length \code{ncol(pmat) - 1}), used to order the rolling sums.}

\item{pmat}{Numeric matrix of allele frequencies, rows = variants,
columns = time points ascending; the first column is the initial
generation.}

\item{N}{Effective population size.}

\item{n}{Numeric vector of sample sizes (chromosomes) per time point,
passed to \code{\link[=covmat_from_pmat]{covmat_from_pmat()}} for the sample size correction.}

\item{take_abs}{Boolean; whether to accumulate the absolute values of the
covariance matrix instead of the raw values.}
}
\value{
A numeric vector, one entry per time point: the corrected G
statistic accumulated up to that time.
}
\description{
Computes G' (Buffalo and Coop style): 1 minus the expected drift-only
variance \verb{times * E[p0(1-p0)] / (2N)}, scaled by the observed rolling sums
of the (optionally absolute-valued) temporal covariance matrix. Values
near 1 indicate covariance consistent with linked selection; values near
or below 0 indicate drift-like, uncorrelated change.
}
\examples{
set.seed(1)
pmat <- matrix(runif(15), nrow = 5, ncol = 3)
g_prime(times = 2:3, pmat = pmat, N = 100, n = c(50, 50, 50))
}
