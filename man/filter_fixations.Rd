% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{filter_fixations}
\alias{filter_fixations}
\title{Filter variants at absorbing allele frequency boundaries}
\usage{
filter_fixations(freq_mat, p0, snp_coords = NULL, tol)
}
\arguments{
\item{freq_mat}{A \code{freq_matrix} object with L rows (variants).}

\item{p0}{A \code{p0_vec} object of length L.}

\item{snp_coords}{Optional \code{snp_coords} object with L rows, subset along
with the other objects so \code{\link[=validate_af_dataset]{validate_af_dataset()}} stays coherent.}

\item{tol}{Single non-negative number: the smallest initial frequency
considered measurable (e.g. \verb{1 / (2N)}); variants with \code{p0 < tol} or
\code{p0 > 1 - tol} are dropped. Required, no default.}
}
\value{
A list with \code{freq_mat}, \code{p0}, and \code{snp_coords} (NULL if not
supplied), subset to variants with usable initial frequencies and with
boundary values in the frequency matrix marked NA.
}
\description{
Removes variants that start near fixation or loss from a paired
\code{freq_matrix} and \code{p0_vec} (and optionally \code{snp_coords}): a variant is
dropped if its initial frequency is within \code{tol} of 0 or 1, or is NA.
Measurement error means such "boundary" frequencies may not be exactly
0 or 1, yet they should be ignored for replicate covariance analyses.
Entries of the kept frequency matrix that sit at or beyond the boundaries
(\code{x <= 0} or \code{x >= 1}, i.e. exact fixation/loss in a validated
\code{freq_matrix}) are marked NA so a follow-up \code{\link[=rm_na_after_na]{rm_na_after_na()}} call per
replicate truncates each trajectory at its first fixation/loss.
}
\examples{
fm <- freq_matrix(matrix(c(0.0, 0.4, 1.0, 0.6), nrow = 2))
p0 <- p0_vec(c(1 / 231, 0.5))
coords <- snp_coords(data.frame(chrom = c("1", "1"), pos = c(100, 200)))
out <- filter_fixations(fm, p0, coords, tol = 1 / 231)
out$p0
out$freq_mat # the fixed/lost entries became NA
}
