% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{multicol_sim}
\alias{multicol_sim}
\title{Simulation to test how multi-collinearity affects estimates}
\usage{
multicol_sim(n, mu1, mu2, b0, b1, b2, covmat)
}
\arguments{
\item{n}{Number of observations to simulate.}

\item{mu1}{Mean of \code{X1}.}

\item{mu2}{Mean of \code{X2}.}

\item{b0}{Intercept of the true model.}

\item{b1}{True coefficient of \code{X1}.}

\item{b2}{True coefficient of \code{X2}.}

\item{covmat}{2x2 covariance matrix of \code{X1} and \code{X2}.}
}
\value{
A data frame with columns \code{term}, \code{value}, and \code{type} (\code{"joint"}
for the joint model, \code{"single"} for the single-predictor models).
}
\description{
Simulates bivariate normal predictors \code{X1} and \code{X2} with covariance matrix
\code{covmat}, generates \code{Y = b0 + b1*X1 + b2*X2 + epsilon}, then fits three
regressions: the joint model (\code{Y ~ X2 + X1}) and each single-predictor
model. Returns the coefficient estimates from all three models stacked
together.
}
\examples{
multicol_sim(
  n = 100, mu1 = 0, mu2 = 0, b0 = 1, b1 = 2, b2 = 3,
  covmat = matrix(c(1, 0.9, 0.9, 1), nrow = 2)
)
}
