% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{snp_coords}
\alias{snp_coords}
\title{Variant coordinates (L variants x 2)}
\usage{
snp_coords(x)
}
\arguments{
\item{x}{A data.frame with L rows and 2 columns: chromosome and position.}
}
\value{
A \code{snp_coords} object: a data.frame with columns \code{CHROM} and \code{POS}.
}
\description{
Constructs a validated variant coordinate table with one row per variant
and the columns \code{CHROM} (character) and \code{POS} (numeric position). Column
names are matched case-insensitively and normalized to uppercase, so the
object plugs directly into \code{\link[=plot_manhattan]{plot_manhattan()}}.
}
\examples{
d <- data.frame(chrom = c("1", "1", "2"), pos = c(100, 250, 80))
snp_coords(d)
}
