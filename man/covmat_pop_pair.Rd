% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/popgen.R
\name{covmat_pop_pair}
\alias{covmat_pop_pair}
\title{Calculate covariances across a pair of environments}
\usage{
covmat_pop_pair(pmat1, pmat2, pop1, pop2, gen0)
}
\arguments{
\item{pmat1}{Numeric matrix of allele frequencies in population 1, rows =
variants, columns = replicate time series. Column names must contain
\code{pop1}.}

\item{pmat2}{Numeric matrix of allele frequencies in population 2, same
layout as \code{pmat1}. Column names must contain \code{pop2}.}

\item{pop1}{Character string identifying population 1; used to grep the
columns of \code{pmat1}.}

\item{pop2}{Character string identifying population 2; used to grep the
columns of \code{pmat2}.}

\item{gen0}{Numeric vector of allele frequencies at generation 0, one per
variant (shared by both populations).}
}
\value{
A single number: the mean standardized between-population
covariance.
}
\description{
Computes the mean standardized covariance of allele frequency changes
between two populations: the average covariance between the two sets of
replicate time series, normalized by the mean pairwise product of
within-population standard deviations.
}
\examples{
gen0 <- c(0.5, 0.4, 0.3)
p1 <- cbind(AA_rep1 = gen0 + 0.01, AA_rep2 = gen0 - 0.01)
p2 <- cbind(BB_rep1 = gen0 - 0.01, BB_rep2 = gen0 + 0.01)
covmat_pop_pair(p1, p2, pop1 = "AA", pop2 = "BB", gen0 = gen0)
}
