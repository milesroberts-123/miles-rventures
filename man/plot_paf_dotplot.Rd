% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_paf_dotplot}
\alias{plot_paf_dotplot}
\title{Dotplot of PAF alignments between two sequences}
\usage{
plot_paf_dotplot(
  paf,
  q_seq,
  t_seq,
  xlab,
  ylab,
  title,
  min_alen = 1000,
  min_mapq = 1
)
}
\arguments{
\item{paf}{A PAF data frame from \code{\link[=read_paf]{read_paf()}}.}

\item{q_seq}{Query sequence name to plot (\code{qname}).}

\item{t_seq}{Target sequence name to plot (\code{tname}).}

\item{xlab}{X-axis label (query).}

\item{ylab}{Y-axis label (target).}

\item{title}{Plot title.}

\item{min_alen}{Minimum alignment block length to plot (default 1000).}

\item{min_mapq}{Minimum mapping quality to plot (default 1).}
}
\value{
A ggplot object. Add highlights (e.g. \code{\link[ggplot2:geom_hline]{ggplot2::geom_hline()}}) and
save it with \code{\link[=save_plot]{save_plot()}}.
}
\description{
Plots alignment blocks from a PAF data frame (as returned by \code{\link[=read_paf]{read_paf()}})
as line segments: query coordinates on the x axis, target coordinates on
the y axis, colored by strand. Reverse-strand blocks are oriented so that
collinear alignments lie on a shared diagonal. Low-quality blocks can be
dropped via \code{min_alen}/\code{min_mapq}.
}
\examples{
paf <- data.frame(
  qname = c("q1", "q1", "q1", "q2"),
  qlen = 10000, qstart = c(100, 500, 100, 100), qend = c(4000, 9000, 4000, 4000),
  strand = c("+", "+", "-", "+"),
  tname = c("t1", "t1", "t1", "t2"),
  tlen = 10000, tstart = c(200, 600, 700, 200),
  tend = c(5000, 10000, 10000, 5000),
  nmatch = c(3900, 4500, 3800, 2900),
  alen = c(3900, 4500, 4000, 3900),
  mapq = c(60, 60, 60, 0)
)
p <- plot_paf_dotplot(paf, q_seq = "q1", t_seq = "t1",
                      xlab = "Query q1", ylab = "Target t1",
                      title = "Demo dotplot")
}
